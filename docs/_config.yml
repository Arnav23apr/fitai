title: FitAI
description: Premium AI-powered fitness app
theme: jekyll-theme-minimal
markdown: kramdown
permalink: /:title/

# Strip the noisy frontmatter / table-of-contents that Jekyll injects so the
# rendered policy reads cleanly. Apple's reviewers prefer policies that look
# like documents, not blogs.
defaults:
  - scope:
      path: ""
    values:
      layout: default
